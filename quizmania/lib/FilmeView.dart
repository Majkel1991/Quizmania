import 'package:flutter/material.dart';

class FilmeView extends StatelessWidget {
  const FilmeView({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Center(child: Text("Filme")));
  }
}
