import 'package:flutter/material.dart';

class GeschichteView extends StatelessWidget {
  const GeschichteView({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Center(child: Text("Geschichte")));
  }
}
