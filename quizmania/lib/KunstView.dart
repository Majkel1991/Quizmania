import 'package:flutter/material.dart';

class KunstView extends StatelessWidget {
  const KunstView({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Center(child: Text("Kunst")));
  }
}
