import 'package:flutter/material.dart';

class LiteraturView extends StatelessWidget {
  const LiteraturView({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Center(child: Text("Literatur")));
  }
}
