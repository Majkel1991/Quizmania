import 'package:flutter/material.dart';

class SportView extends StatelessWidget {
  const SportView({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Center(child: Text("Sport")));
  }
}
